{
  "name": "sarqt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Community food-sharing — dev tooling (site itself is buildless static)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
